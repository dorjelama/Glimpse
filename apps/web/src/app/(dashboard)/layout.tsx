// Route group layout — passthrough. Sidebar is provided by DashboardShell component.
export default function DashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
